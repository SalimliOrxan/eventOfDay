<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_marginTop="8dp"
        android:layout_height="wrap_content"
        android:background="@drawable/button_back"
        app:cardElevation="3dp" app:cardCornerRadius="5dp">

    <androidx.constraintlayout.widget.ConstraintLayout
            android:minHeight="50dp"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

        <TextView
                android:text="@string/itemAdded"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:gravity="start|center"
                android:padding="5dp"
                android:id="@+id/itemAdded" app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintEnd_toStartOf="@+id/remove" android:layout_marginEnd="16dp"
                app:layout_constraintBottom_toBottomOf="parent"/>

        <ImageButton
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:srcCompat="@drawable/ic_remove"
                android:background="@null"
                android:scaleType="fitXY"
                android:tint="@color/buttonColor"
                android:id="@+id/remove"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="@+id/itemAdded"
                app:layout_constraintBottom_toBottomOf="@+id/itemAdded"
                android:contentDescription="@string/remove" android:layout_marginEnd="3dp"/>

    </androidx.constraintlayout.widget.ConstraintLayout>


</androidx.cardview.widget.CardView>